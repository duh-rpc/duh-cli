openapi: 3.0.0
info:
  title: Missing Request Body
  version: 1.0.0
paths:
  /users.create:
    post:
      # Missing requestBody entirely
      responses:
        200:
          description: Success
          content:
            application/json:
              schema:
                type: object
  /users.update:
    post:
      requestBody:
        required: false  # Should be true
        content:
          application/json:
            schema:
              type: object
      responses:
        200:
          description: Success
          content:
            application/json:
              schema:
                type: object
