openapi: 3.0.0
info:
  title: Missing Server Version
  version: 1.0.0
servers:
  - url: https://api.example.com
paths:
  /users.create:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        200:
          description: Success
          content:
            application/json:
              schema:
                type: object
