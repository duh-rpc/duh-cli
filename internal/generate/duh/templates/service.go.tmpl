// Code generated by 'duh generate --full' on {{.Timestamp}}. YOU CAN EDIT.

package {{.Package}}

import (
	"context"
	"log/slog"
{{if .IsFullTemplate}}	"sort"
	"strconv"
	"sync"

	"github.com/duh-rpc/duh.go/v2"
	pb "{{.ProtoImport}}"
	"github.com/google/uuid"
	"github.com/kapetan-io/tackle/set"
	"google.golang.org/protobuf/types/known/timestamppb"
{{else}}
	"github.com/duh-rpc/duh.go/v2"
	pb "{{.ProtoImport}}"
{{end}})

type ServiceConfig struct {
	InstanceID string
	Log        *slog.Logger
}

type Service struct {
{{if .IsFullTemplate}}	users map[string]*pb.GetResponse
	mutex sync.RWMutex
{{end}}	conf  ServiceConfig
}

func NewService(conf ServiceConfig) (ServiceInterface, error) {
{{if .IsFullTemplate}}	set.Default(&conf.Log, slog.Default())

	return &Service{
		users: make(map[string]*pb.GetResponse),
		conf:  conf,
	}, nil
{{else}}	return &Service{conf: conf}, nil
{{end}}}

{{range .Operations}}
{{if .IsInitTemplateMethod}}
{{if eq .Path "/users.create"}}
func (s *Service) {{.MethodName}}(ctx context.Context, req *{{.RequestType}}, resp *{{.ResponseType}}) error {
	if req.Email == "" {
		return duh.NewServiceError(duh.CodeBadRequest, "email is required", nil, nil)
	}
	if req.Name == "" {
		return duh.NewServiceError(duh.CodeBadRequest, "name is required", nil, nil)
	}

	s.mutex.Lock()
	defer s.mutex.Unlock()

	for _, user := range s.users {
		if user.Email == req.Email {
			return duh.NewServiceError(duh.CodeBadRequest, "user with this email already exists", nil, nil)
		}
	}

	now := timestamppb.Now()
	userID := uuid.New().String()

	user := &pb.GetResponse{
		CreatedAt: now,
		UserId:    userID,
		Email:     req.Email,
		Name:      req.Name,
		Age:       req.Age,
	}

	s.users[userID] = user

	resp.CreatedAt = now
	resp.UserId = userID
	resp.Email = req.Email
	resp.Name = req.Name

	return nil
}
{{else if eq .Path "/users.get"}}
func (s *Service) {{.MethodName}}(ctx context.Context, req *{{.RequestType}}, resp *{{.ResponseType}}) error {
	if req.UserId == "" {
		return duh.NewServiceError(duh.CodeBadRequest, "user_id is required", nil, nil)
	}

	s.mutex.RLock()
	defer s.mutex.RUnlock()

	user, exists := s.users[req.UserId]
	if !exists {
		return duh.NewServiceError(duh.CodeNotFound, "user not found", nil, nil)
	}

	resp.CreatedAt = user.CreatedAt
	resp.UserId = user.UserId
	resp.Email = user.Email
	resp.Name = user.Name
	resp.Age = user.Age

	return nil
}
{{else if eq .Path "/users.list"}}
func (s *Service) {{.MethodName}}(ctx context.Context, req *{{.RequestType}}, resp *{{.ResponseType}}) error {
	s.mutex.RLock()
	defer s.mutex.RUnlock()

	const defaultFirst = 10
	first := defaultFirst
	if req.Pagination != nil && req.Pagination.First > 0 {
		first = int(req.Pagination.First)
	}

	offset := 0
	if req.Pagination != nil && req.Pagination.After != "" {
		if n, err := strconv.Atoi(req.Pagination.After); err == nil {
			offset = n
		}
	}

	users := make([]*pb.GetResponse, 0, len(s.users))
	for _, user := range s.users {
		users = append(users, user)
	}

	switch req.SortBy {
	case pb.SortBy_SORT_BY_NAME:
		sort.Slice(users, func(i, j int) bool {
			return users[i].Name < users[j].Name
		})
	case pb.SortBy_SORT_BY_EMAIL:
		sort.Slice(users, func(i, j int) bool {
			return users[i].Email < users[j].Email
		})
	case pb.SortBy_SORT_BY_CREATED_AT:
		sort.Slice(users, func(i, j int) bool {
			return users[i].CreatedAt.AsTime().Before(users[j].CreatedAt.AsTime())
		})
	}

	available := users[offset:]
	end := first
	if end > len(available) {
		end = len(available)
	}

	resp.Items = available[:end]
	resp.Pagination = &pb.PaginationResponse{
		HasMore: end < len(available),
	}
	if resp.Pagination.HasMore {
		resp.Pagination.EndCursor = strconv.Itoa(offset + end)
	}

	return nil
}
{{else if eq .Path "/users.update"}}
func (s *Service) {{.MethodName}}(ctx context.Context, req *{{.RequestType}}, resp *{{.ResponseType}}) error {
	if req.UserId == "" {
		return duh.NewServiceError(duh.CodeBadRequest, "user_id is required", nil, nil)
	}

	s.mutex.Lock()
	defer s.mutex.Unlock()

	user, exists := s.users[req.UserId]
	if !exists {
		return duh.NewServiceError(duh.CodeNotFound, "user not found", nil, nil)
	}

	if req.Email != "" {
		for id, u := range s.users {
			if id != req.UserId && u.Email == req.Email {
				return duh.NewServiceError(duh.CodeBadRequest, "user with this email already exists", nil, nil)
			}
		}
		user.Email = req.Email
	}

	if req.Name != "" {
		user.Name = req.Name
	}

	if req.Age != 0 {
		user.Age = req.Age
	}

	now := timestamppb.Now()

	statusStr := req.Status.String()

	resp.UpdatedAt = now
	resp.CreatedAt = user.CreatedAt
	resp.UserId = user.UserId
	resp.Email = user.Email
	resp.Name = user.Name
	resp.Age = user.Age
	resp.Status = statusStr

	return nil
}
{{end}}
{{else}}
func (s *Service) {{.MethodName}}(ctx context.Context, req *{{.RequestType}}, resp *{{.ResponseType}}) error {
	return duh.NewServiceError(duh.CodeNotImplemented, "{{.MethodName}} not implemented", nil, nil)
}
{{end}}
{{end}}

func (s *Service) Shutdown(ctx context.Context) error {
{{if .IsFullTemplate}}	s.mutex.Lock()
	defer s.mutex.Unlock()

	s.conf.Log.Info("Shutting down service", "users", len(s.users))
	s.users = nil
{{else}}	// TODO: Cleanup resources
{{end}}	return nil
}